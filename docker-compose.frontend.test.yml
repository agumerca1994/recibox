services:
  recibox-frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_APP_ENV: test
        VITE_FIREBASE_AUTH_ENABLED: ${VITE_FIREBASE_AUTH_ENABLED:-false}
        VITE_FIREBASE_ANALYTICS_ENABLED: ${VITE_FIREBASE_ANALYTICS_ENABLED:-false}
        VITE_FIREBASE_API_KEY: ${VITE_FIREBASE_API_KEY:-}
        VITE_FIREBASE_AUTH_DOMAIN: ${VITE_FIREBASE_AUTH_DOMAIN:-}
        VITE_FIREBASE_PROJECT_ID: ${VITE_FIREBASE_PROJECT_ID:-}
        VITE_FIREBASE_STORAGE_BUCKET: ${VITE_FIREBASE_STORAGE_BUCKET:-}
        VITE_FIREBASE_MESSAGING_SENDER_ID: ${VITE_FIREBASE_MESSAGING_SENDER_ID:-}
        VITE_FIREBASE_APP_ID: ${VITE_FIREBASE_APP_ID:-}
        VITE_FIREBASE_MEASUREMENT_ID: ${VITE_FIREBASE_MEASUREMENT_ID:-}
    restart: unless-stopped
    environment:
      API_UPSTREAM: ${API_UPSTREAM:-https://api-test.recibox.com.ar}
      API_UPSTREAM_HOST: ${API_UPSTREAM_HOST:-api-test.recibox.com.ar}
      VITE_APP_ENV: test
    expose:
      - "80"
    networks:
      - easypanel

networks:
  easypanel:
    external: true
