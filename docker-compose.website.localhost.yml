services:
  recibox-website:
    build:
      context: .
      dockerfile: website/Dockerfile
    restart: unless-stopped
    ports:
      - "${WEBSITE_PORT:-8090}:80"
    networks:
      - recibox-local

networks:
  recibox-local:
    driver: bridge
