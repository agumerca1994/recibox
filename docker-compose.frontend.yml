services:
  recibox-frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - easypanel

networks:
  easypanel:
    external: true